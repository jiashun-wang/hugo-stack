{
  "name": "hugo-stack-site",
  "version": "1.0.0",
  "scripts": {
    "prebuild": "if [ ! -f hugo ]; then echo 'Installing Hugo v0.157.0...'; wget -q -O hugo.tar.gz https://github.com/gohugoio/hugo/releases/download/v0.157.0/hugo_extended_0.157.0_linux-amd64.tar.gz && tar -xzf hugo.tar.gz && rm hugo.tar.gz; fi",
    "build": "git submodule update --init --recursive && ./hugo --minify",
    "clean": "rm -rf public resources hugo",
    "update": "git submodule update --remote --recursive"
  }
}