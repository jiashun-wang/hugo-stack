{
  "name": "hugo-theme-stack",
  "version": "1.0.0",
  "scripts": {
    "prebuild": "if [ ! -f hugo ]; then echo 'Installing Hugo...'; curl -L -o hugo.tar.gz https://github.com/gohugoio/hugo/releases/download/v0.107.0/hugo_extended_0.107.0_linux-amd64.tar.gz; tar -xzf hugo.tar.gz hugo; chmod +x hugo; rm hugo.tar.gz; fi",
    "build": "./hugo --minify",
    "dev": "./hugo server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^3.8.1",
    "prettier-plugin-go-template": "^0.0.15"
  }
}