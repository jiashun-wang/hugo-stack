{
  "name": "hugo-stack-site",
  "version": "1.0.0",
  "scripts": {
    "prebuild": "if [ ! -f hugo ]; then echo 'Installing Hugo v0.157.0...'; curl -sSL https://github.com/gohugoio/hugo/releases/download/v0.157.0/hugo_extended_0.157.0_linux-amd64.tar.gz -o hugo.tar.gz && tar -xzf hugo.tar.gz && rm hugo.tar.gz; fi",
    "build": "./hugo --minify",
    "clean": "rm -rf public resources hugo",
    "dev": "./hugo server -D"
  }
}