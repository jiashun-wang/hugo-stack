{
  "name": "hugo-stack-site",
  "version": "1.0.0",
  "scripts": {
    "prebuild": "echo 'Using EdgeOne built-in Hugo, skipping installation.'",
    "build": "hugo --minify",
    "clean": "rm -rf public resources"
  }
}